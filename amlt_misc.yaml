description: misc

environment:
  image: jzbjyb/my-repo:latest

target:
  service: amlk8s
  name: itp-scus-v100
  vc: AlexTScience

storage:
  data:
    storage_account_name: tsinterns
    container_name: t-zhjiang
    mount_dir: /mnt/root

code:
  local_dir: $CONFIG_DIR

jobs:
- name: whole_faiss
  sku: C40
  sku_count: 1
  command:
  - ./whole_faiss.sh /mnt/root/TaBERT/data/runs/tabert_base_k1/index/tapas_data0_whole_avg_cell/repr.npz
- name: gen_data
  sku: C40
  sku_count: 1
  command:
  - ./gen_vanilla.sh
